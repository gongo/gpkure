source :rubygems

gem 'sinatra'
gem 'thin'
gem 'haml'
gem 'redis'

group :development do
  gem 'shotgun'
end

group :test do
  gem 'rack-test'
  gem 'mock_redis', :git => "git://github.com/causes/mock_redis.git"
end
